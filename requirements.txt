# ═══════════════════════════════════════════════════════════════════
# Breast Cancer Histopathology - Complete Requirements
# ═══════════════════════════════════════════════════════════════════
# For A100 GPU servers with CUDA 11.8+
# Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────
# Core Deep Learning Frameworks (A100 Optimized)
# ───────────────────────────────────────────────────────────────────

# PyTorch with CUDA 11.8 support (install first!)
# Command: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.1.0
torchvision>=0.16.0

# ───────────────────────────────────────────────────────────────────
# Transformer Models & Pretrained Weights
# ───────────────────────────────────────────────────────────────────

# TIMM - PyTorch Image Models (Swin, ConvNeXt, DeiT, ViT)
timm>=1.0.0

# ───────────────────────────────────────────────────────────────────
# Data Processing & Utilities
# ───────────────────────────────────────────────────────────────────

# Image processing
Pillow>=10.0.0

# Data manipulation
numpy>=1.24.0
pandas>=2.0.0

# Configuration
pyyaml>=6.0

# Progress bars
tqdm>=4.65.0

# HTTP requests (for dataset download)
requests>=2.31.0

# OpenCV for image processing
opencv-python>=4.8.0

# ───────────────────────────────────────────────────────────────────
# Machine Learning & Metrics
# ───────────────────────────────────────────────────────────────────

# Scikit-learn for metrics and splits
scikit-learn>=1.3.0

# Scipy for statistical tests
scipy>=1.11.0

# ───────────────────────────────────────────────────────────────────
# Visualization
# ───────────────────────────────────────────────────────────────────

# Plotting libraries
matplotlib>=3.7.0
seaborn>=0.12.0

# ───────────────────────────────────────────────────────────────────
# Quantum Computing (GPU-Accelerated)
# ───────────────────────────────────────────────────────────────────

# PennyLane for quantum circuits (fallback)
pennylane>=0.32.0
pennylane-lightning>=0.32.0

# TorchQuantum (optional - GPU-accelerated quantum)
# Uncomment if using: pip install torchquantum
# torchquantum>=0.1.3

# ───────────────────────────────────────────────────────────────────
# Model Profiling & Analysis
# ───────────────────────────────────────────────────────────────────

# FLOPs and MACs computation
ptflops>=0.7.2

# Model complexity analysis
thop>=0.1.1

# ───────────────────────────────────────────────────────────────────
# A100 GPU Optimizations
# ───────────────────────────────────────────────────────────────────

# Flash Attention 2 (A100 specific - significant speedup)
# Install: pip install flash-attn --no-build-isolation
flash-attn>=2.3.0

# ───────────────────────────────────────────────────────────────────
# Development & Experiment Tracking
# ───────────────────────────────────────────────────────────────────

# Jupyter for notebooks
jupyterlab>=4.0.0
ipykernel>=6.25.0

# Experiment tracking (REQUIRED)
wandb>=0.16.0

# ───────────────────────────────────────────────────────────────────
# Installation Notes for A100 Servers
# ───────────────────────────────────────────────────────────────────
#
# 1. Install PyTorch with CUDA 11.8:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 2. Install Flash Attention (optional but recommended for A100):
#    pip install flash-attn --no-build-isolation
#
# 3. Install TorchQuantum (optional for quantum):
#    git clone https://github.com/mit-han-lab/torchquantum.git
#    cd torchquantum && pip install -e .
#
# 4. Verify GPU setup:
#    python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}, GPU: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else \"N/A\"}')"
#
# ═══════════════════════════════════════════════════════════════════
